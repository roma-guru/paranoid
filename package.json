{
  "name": "paranoid",
  "version": "0.0.2",
  "description": "P2P Encryption in social networks",
  "main": "bundle.js",
  "repository": "github.com/roma-guru/paranoid",
  "author": "Roman Voropaev <voropaev.roma@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "jsencrypt": "^3.0.0-rc.1",
    "jshashes": "^1.0.8"
  },
  "scripts": {
    "test": "npx jasmine --config=tests/jasmine.json",
    "build": "npx browserify main.js -o dist/bundle.js",
    "serve": "web-ext run -u vk.com/im -p Paranoid --keep-profile-changes",
    "package": ". .env && web-ext sign -a dist --api-key $JWT_ISSUER --api-secret $JWT_TOKEN"
  },
  "devDependencies": {
    "browserify": "^16.5.0",
    "jasmine": "^3.5.0",
    "jsdom": "^16.2.2",
    "web-ext": "^4.2.0"
  }
}
